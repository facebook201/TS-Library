/**
 * 贪吃蛇
 * 
 */ 


#include <stdlib.h>
// #include <con.h>

int main() {
  // 地图
  // f 食物 m 地图 S 地图大小 W 地图尺寸 l 长度 c 动作 z 坐标
  int W = 15, S = W * W, *m, z[2] = {0}, l = 3, c = 'D', C, i, f, *p;

  for (srand(m = calloc(S, 4)), ; ;) {

  }
}
